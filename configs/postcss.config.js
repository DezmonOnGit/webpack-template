const autoprefixer = require('autoprefixer');

// module.exports = {
//     plugins: [autoprefixer({ grid: true }), require("cssnano")]
// };

module.exports = {
    plugins: [autoprefixer()]
};
